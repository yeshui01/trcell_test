package wsserve

import "testing"

func TestWSServer(t *testing.T) {
	// serv := NewWSServer()
	// serv.InitRouter("/wstest")
	// stopCh := make(chan bool)
	// serv.Run("localhost:7200", stopCh, 0)
}
